include"main.h"

/**
print_times_table the ntimes table
*
*Return: void
*/

void print_times_table(int n)
{
        int i, j, k;
        if (n >= 0; && n <= 15)
        {
                 for (j = 0; j <= n; j++)
                 {
                         k = i * j;
                         if (i == 0 && j == 0)
                                  _putchar('0');
                          else if (k == 0)

